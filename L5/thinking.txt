Thinking1  什么是矩阵分解，都有哪些应用场景？
用户对物品的评分可以表示成二维矩阵，大部分用户对物品是没有评分，需要根据矩阵中已有的值来补全未知值，矩阵分解是为了补全矩阵，矩阵分解把矩阵分解成用户和物品矩阵来找到用户和物品的隐含特征，两个矩阵相乘可以补全缺失值
应用场景主要有评分预测和Top-N推荐

Thinking2  矩阵分解算法ALS-WR是如何进行的？
引入置信度，对于用户对物品有偏好的赋予较大的权重，用户对物品没有行为的赋予较小的权重，通过最小二乘法求解xu和yi，固定Y优化X，固定X优化Y，重复直到X和Y都收敛

Thinking3	 梯度下降法中的批量梯度下降（BGD），随机梯度下降（SGD），和小批量梯度下降有什么区别（MBGD
批量梯度下降：在每次更新时用所有样本，稳定，收敛慢
随机梯度下降：每次更新时用1个样本，用1个样本来近似所有的样本，更快收敛，最终解在全局最优解附近
小批量梯度下降：每次更新时用b个样本，折中方法，速度较快

Thinking4  推荐系统中的冷启动都有哪些情况，有哪些常用的解决方法
有物品冷启动、用户冷启动、系统冷启动
常用方法：
1.提供非个性化的推荐(用户冷启动)：推荐热门商品、人工指定策略、提供多样性的选择
2.利用用户注册信息(用户冷启动、系统冷启动)基于用户信息，比如年龄，性别，地域、学历、职业等做推荐；让用户选择兴趣点选择自己喜欢的分类标签；利用社交关系，将好友喜欢的商品推荐
3.基于内容做推荐(用户冷启动、系统冷启动)：当用户行为较少时，无法使用协同过滤，可以基于item本身的内容做推荐
4.快速试探策略(用户冷启动、标的物冷启动)：随机或者按照非个性化推荐的策略进行推荐，基于用户的点击反馈快速发现兴趣点，用于新闻、短视频领域
5.兴趣迁移策略(用户冷启动、系统冷启动)：借鉴了迁移学习的思路，比如基于头条给抖音导流，知道用户在头条上的兴趣点，可以为其进行推荐
6.利用商品的metadata信息做推荐（商品冷启动）：
      利用Item与用户行为的相似性：计算新商品的特征，再计算商品特征与用户行为特征之间的相似度，从而为商品推荐和它最匹配的用户
      利用Item与Item的相似性：可以基于Item的属性信息来做推荐，一般新上线的商品都会有一些属性，根据这些属性找到与最相似的商品，这些相似的商品被哪些用户“消费”过，可以将该item推荐给这些消费过的用户。

Thinking5  你阅读过和推荐系统，机器学习相关的论文么？有哪些论文是你比较推荐的，可以分享到微信群中
阅读过slope one
